# Jido (自動)

## A Python CLI tool that automates the creation of Japanese Anki flashcards

This program is designed to automate the creation of Japanese Anki flashcards with minimal user input. This is done either word-by-word through manual input, or by importing a CSV file. When ready, it will produce a .apkg file which can then be imported into Anki. The data included on the cards are as follows:

* The expression, as typed, including words usually written in kana but that have rarely-used kanji
* The meaning of the expression
* The reading of the expression, including furigana
* The pitch accent of the expression, including an SVG diagram
* A sentence in Japanese with surrounding context at a level designated by the user (i.e., JLPT N4)
* An English translation of the Japanese sentence
* Audio of the expression
* Audio of the Japanese sentence
* Additional notes and information

Furthermore, two separate cards are created for each word: one in which the user is tested by the expression, and one where they are tested by the audio of both the expression and the Japanese sentence.

## Requirements

* Python 3.12+
* Anthropic API key
* Microsoft Azure Cognitive Services API key and endpoint

## Features

* Study level selection (currently JLPT N5, Genki, Quartet) for sentence generation difficulty
* Batch CSV import with session checkpoints
* Automatic する and な suffix stripping for verb and adjective input
* Kana input normalization: words usually written in kana (i.e., ありがとう) but that have rarely-used kanji forms (i.e., 有り難う) are correctly resolved
* SQLite database caching via a FastAPI backend: cards are stored after generation and retrieved on subsequent lookups, avoiding redundant API calls
* Per-session log file with a summary and detailed word-by-word status

## Future plans

This project is entirely usable in its current state but there are future improvements planned, including but not limited to the following:

### Concurrency

The next update will incorporate concurrency where possible to allow either multiple parts of a card to be generated simultaneously or potentially multiple cards in order to decrease the amount of time required even further.

### Subtitle and text import via NLP

A future phase of this project is the addition of a morphological analysis of text files (i.e., subtitle files). This would allow a user to upload a text file and select a percentage of words (i.e., top 85%), and the program would analyze the file and determine that percentage of the most common words excluding particles, auxiliary verbs, etc. It would then use surrounding context to determine the meanings of each word and generate an Anki deck containing those words.

### Browser integration

A potential future feature is a lightweight browser extension allowing users to look up Japanese words while browsing and send them directly into Jido's pipeline for card creation. 

### Proper CLI interface and website

Jido is currently deployed on a self-hosted home server only accessible locally. The long-term goal is to open it to public use through a proper CLI interface and eventually a web application, which will require addressing authentication and network security first.

## Known issues

There are several currently known issues of varying degrees, including the following:

* The pitch accent data is not 100% accurate. No reliable method has been found to meaningfully ensure accurate pitch accent data because it can vary based on the meaning of a word.
* The sentences generated by Anthropic API may not always be perfect. This is an inevitable result of using an LLM but improvements to the prompts are constantly being considered.
* The audio for some expressions is very rarely mispronounced. 

## Attributions

All of the data used in this program is obtained from the following sources:

* [Unofficial Jisho API](https://github.com/mistval/unofficial-jisho-api) (mistval) — meaning, reading, notes
* [JMdict/EDICT](https://www.edrdg.org/jmdict/j_jmdict.html) (Electronic Dictionary Research and Development Group)
* [Kanjium](https://github.com/mifunetoshiro/kanjium) (mifunetoshiro) — pitch accent data, CC BY-SA 4.0
* [JmdictFurigana](https://github.com/Doublevil/JmdictFurigana) (Doublevil) — furigana data, CC BY-SA 4.0
* [genanki](https://github.com/kerrickstaley/genanki) (kerrickstaley) — Anki integration
* Anthropic API — sentence data
* Microsoft Azure Cognitive Services — expression and Japanese sentence audio